# React Native

This is the Expo Development Build version of Shader Gadgets. It uses React
Native WebGPU for rendering and TypeGPU for the shader pipeline.

It does not run in Expo Go because WebGPU requires native modules.

## Setup

```sh
cd react-native
npm install
npm run ios
```

For Android, use:

```sh
npm run android
```
