# SwiftUI

This is the SwiftUI version of Shader Gadgets. It uses SwiftUI for the app UI
and Metal shaders for the rendering effects.

## Setup

Open the project in Xcode:

```sh
open SwiftUI/ShaderGadgets.xcodeproj
```

Select the `ShaderGadgets` scheme, choose a simulator or device, and run the
app.
